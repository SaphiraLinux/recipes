#!/sbin/openrc-run

description="ISC BIND nameserver"
command=/usr/sbin/named
command_args="-u named -c /etc/bind/named.conf"
pidfile=/run/named/named.pid

depend()
{
	need localmount net
	before netmount
	provide dns
}

start_pre()
{
	checkpath -d -m 0755 -o named:named /run/named
}
