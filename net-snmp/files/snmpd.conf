# Saphira default snmpd.conf: local monitoring only. snmpd runs as the
# unprivileged snmp account (see the service units); widen access
# deliberately by editing this file.
rocommunity public localhost
syslocation Saphira router
syscontact root@localhost
