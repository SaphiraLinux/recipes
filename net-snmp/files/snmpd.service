[Unit]
Description=Net-SNMP agent
Documentation=man:snmpd(8)
After=network.target

[Service]
Type=simple
User=snmp
Group=snmp
AmbientCapabilities=CAP_NET_BIND_SERVICE
ExecStart=/usr/sbin/snmpd -f -C -c /etc/snmp/snmpd.conf -u snmp -g snmp
Restart=on-failure

[Install]
WantedBy=multi-user.target
