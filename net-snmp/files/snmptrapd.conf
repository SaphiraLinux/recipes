# Saphira default snmptrapd.conf: accept and log traps locally.
# disableAuthorization is the upstream default posture for a trap
# receiver; restrict with authCommunity/trap2sink credentials when the
# network needs it (see snmptrapd.conf(5)).
disableAuthorization yes
