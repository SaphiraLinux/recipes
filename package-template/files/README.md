# package-template

This package is a copy-me starting point for hand-authoring new Saphira
packages. It installs nothing useful; it exists so that a full recipe
tree never contains a non-buildable directory.

See /recipes/package-template/recipe.sh for the step-by-step checklist.
